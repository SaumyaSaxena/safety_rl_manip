tag: ${now:%Y-%m-%d-%H-%M-%S}
debug: True # TODO: always true since we are not using wandb during eval

eval_cfg:
  gpu: 0
  wandb_load:
    run_path: 'iam-lab/safety_rl_manip/96z2aq34'
    file: model/step_591999_test_return_1.75.pth