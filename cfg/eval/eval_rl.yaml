tag: ${now:%Y-%m-%d-%H-%M-%S}
debug: True # TODO: always true since we are not using wandb during eval

eval_cfg:
  gpu: 0
  wandb_load:
    run_path: iam-lab/safety_rl_manip/rarsf8gp
    file: model/step_71999_test_return_-0.69.pth
  eval_value_fn: True
  eval_safe_rollouts: True
  save_rollout_gifs: True
  num_eval_episodes: 5000
  seed: 30