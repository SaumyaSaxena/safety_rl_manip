tag: ${now:%Y-%m-%d-%H-%M-%S}
debug: True # TODO: always true since we are not using wandb during eval

eval_cfg:
  gpu: 0
  wandb_load:
    run_path: 'iam-lab/safety_rl_manip/efdjkgio'
    file: model/step_375999_test_return_1.51.pth