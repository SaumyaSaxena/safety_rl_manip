tag: ${now:%Y-%m-%d-%H-%M-%S}
debug: True # TODO: always true since we are not using wandb during eval

eval_cfg:
  gpu: 0
  wandb_load:
    run_path: 'iam-lab/safety_rl_manip/yxw2qyca'
    file: model/step_791999_test_return_2.01.pth